# Release-readiness snapshots

Files in this directory are immutable, dated or versioned evidence generated
from the live Portcove Roadmap and `catalog.json` before a tagged release. They
do not become the planning authority after generation. See
[`docs/RELEASING.md`](../RELEASING.md) and generate a new snapshot when the
release state changes.
