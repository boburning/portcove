# Deferred campaign disposition — 2026-09-03 (historical)

> **Historical evidence.** This dated disposition is preserved as a release
> and qualification record. It is not current planning authority; use the live
> Portcove Roadmap for active work, priority, and blockers.

This is the final-disposition index for the campaign, with detailed chronological evidence in [the campaign log](2026-09-03-deferred-campaign.md), the former queue in [the archived deferred ledger](2026-09-03-deferred-ledger.md), and the fresh [direct-upstream inventory](2026-09-03-deferred-upstreams.md). Qualification libraries, reports, owned sources, snapshots, crash diagnostics, and third-party binaries remain private local evidence under ignored `work/` or the user's original source locations. No release, tag, or production deployment is part of this campaign.

## Completed items and capabilities

| Item | Change and completion evidence | Principal files | Commit |
|---|---|---|---|
| PCV-DEF-001 | Public Portcove GitHub App registered without a client secret/private key or added repository permissions. The user completed authorization, GUI logout/login, and fresh-start connected-account checks; independent CLI processes read the secure-store credential. Anonymous use remains supported. | `crates/portcove-core/src/auth.rs`, GitHub documentation and tests | `1b461bc` |
| PCV-DEF-010 | Already complete when revalidated. The disposable visual-QA cleanup and its deferral removal were merged; no ambiguous/current worktree or qualification data was deleted by this campaign. | `docs/DEFERRED.md` and prior cleanup evidence | `8eb6881` (#9) |
| PCV-DEF-005 engineering | One controller navigation model, visible focus, row/group traversal, modal containment, nested Back, choice lists, sidebar return, equal header control heights, clearer hints/theme copy, and native external links. User Xbox feedback progressed from failure to improved feel; Move library containment and focus return passed physically. Compact/nested acceptance remains below. | `apps/desktop/src/gamepad.ts`, `focus.ts`, `dialog.ts`, navigation components/styles/tests, `apps/desktop/src-tauri/src/lib.rs` | `cea65ec`, `0744a12`; later modal corrections accompany portability/cancellation |
| PCV-DEF-007: Paper Mario ReCut | Windows application/source/lifecycle automation complete: real old/current release pair, independent adoption, stage/activate/verify/rollback/reuse, preserved original, generated-data remove/reinstall, clean process exit, and post-move recovery. Animated opening observed; real save/gameplay/audio acceptance remains PCV-DEF-003. | core catalog, `install.rs`, `service.rs`, catalog tests, `docs/CATALOG.md`, `docs/V1-CUTOFF.md` | `8280a2f` |
| PCV-DEF-007: Mega Man X6 | Real v1.0.7/v1.0.9 release pair and full lifecycle passed. Strict DOS ZIP path normalization enabled the upstream archive without relaxing traversal/collision checks. Reviewed launch arguments anchor memory cards in the managed version. Both generated cards survived removal/reinstall and launch-time restoration with identical SHA-256. | `archive.rs`, core catalog and tests, qualification docs | `3d1b971`, `8280a2f` |
| PCV-DEF-007: reusable runtimes | Core verifies pinned runtime downloads and extraction inside the game install transaction. Schema-3 manifests and schema-12 SQLite retain exact runtime identity through adoption, stage/reuse, rollback, import, and signed catalog refreshes. Every immutable file is critical. Synthetic rejection, lifecycle, recovery, and provenance tests pass. All four vendor Corretto archives matched their published SHA-256. | `runtime.rs`, `install.rs`, `service.rs`, catalog/schema/library/transfer modules; CLI and desktop DTO/presentation adapters | `8280a2f` |
| PCV-DEF-009/011/012 tooling | Repeatable isolated installer replacement with complete bundled-executable hashing, clean startup/shutdown, preserved library/marker, and uninstall cleanup. A bounded qualification report records exact CLI identity, core diagnostics, backup identities, and explicitly unassessed human observations. | `scripts/test-windows-installer.ps1`, `scripts/qualification-report.mjs`, `docs/RELEASING.md` | `3b79568` |
| PCV-DEF-012 restore correction | A restored canonical snapshot now synchronizes every registered version, removes paths absent from that snapshot, and clears stale launch markers. Interrupted synchronization retains its recovery journal and blocks launch/save collection. A one-use authorization admits expensive work before its deadline while still rejecting changed state. Windows and Linux regressions cover launch, rollback, staged activation, interruption, safety contents, replay and symlinks. | core `authorization.rs`, `library.rs`, `operation.rs`, `recovery.rs`, `service.rs`; `docs/ARCHITECTURE.md` | `24666f1` |
| PCV-DEF-011 presentation corrections | New tasks no longer display a completed update's label/progress. Cartridge pickers expose the ZIPs core already validates, while disc/folder profiles retain their specific filters. Real Settings selection rejected the wrong cartridge ZIP, kept the missing requirement visible, and accepted the correct Paper Mario ZIP. | `apps/desktop/src/components/Chrome.tsx`, `file-picker.ts`, component/picker tests | `1d8a4e3` |
| PCV-DEF-007: six additional Windows qualifications | Severed Chains, Dinosaur Planet, Trouble Makers, Virtual Pro Wrestling 64, WWF WrestleMania 2000 and Space Station Silicon Valley passed managed lifecycle, visible launch, removal/reinstall and immutable verification. Dino/SSSV required Vulkan on this AMD PC. VPW64/WM2000 have only one upstream release, so their evidence is same-release adoption/download, not an invented historical pair. DKR-R's newly supplied exact source is now accepted; native platform qualification remains. | core catalog, `runtime_tests.rs`, catalog/cutoff/qualification docs | `eb4b6f0` |
| PCV-DEF-007: three OpenGOAL Windows qualifications | Jak and Daxter, Jak II and Jak 3 passed accepted-retail-source setup plus real v0.3.5-to-v0.3.6 adoption, failed-download recovery, staging, activation, rollback, retained reuse, removal/reinstall, fresh setup, visible launch, clean supervision, immutable verification, persistent-marker preservation and unchanged-source checks. Generated data is now manifest-owned; reviewed logs and `imgui.ini` remain disposable runtime output. | core adapter/install/library/service/catalog contracts and qualification docs | This completion commit |
| PCV-DEF-007: NocturneRecomp and Gen2Recomped | Vanilla NocturneRecomp is cataloged for Windows with bounded read-only LIVE/STFS extraction, exact package/default-XEX identity, transactional publication, fixed updater/data roots and canonical Portcove user data. Its exact v1.4.4 tree was adopted before the real v1.4.5 release was staged, activated, verified, rolled back, verified, reactivated, removed and reinstalled. The v1.4.5 launch was responsive; all 216 extracted files and 98,788,397 bytes exactly matched an independent extraction, and a profile save appeared under the isolated user root. Closing the native window returned nonzero, so the launch activity remains honestly failed even though post-exit immutable and source verification passed. Gen2Recomped Gold, Silver, and Crystal are separate opt-in beta Windows entries with exact ROM identities and fixed game selectors; release integrity and static contracts pass, while real imports await matching owned ROMs. | core `stfs.rs`, adapter/catalog/types contracts, desktop DTO, catalog/cutoff/qualification docs | This completion commit |
| PCV-DEF-012: exact persistent ownership | Named saves, quick/auto states and profile deletion now survive restore and version transitions (manifest 4/API 9). Absolute durable paths remove dependence on the caller's directory. Real Picori testing also caught an undeclared copied source; `baserom.gba` now belongs to persistent data, and copy contracts without an owning path fail catalog validation. | `catalog.rs`, core catalog, persistence/version-transition modules and tests, `docs/ARCHITECTURE.md` | `f9c83e6`, `7a69d27`; `eb4b6f0` |
| PCV-DEF-004: rejected credential recovery | A saved GitHub App credential returned HTTP 401. Settings now keeps its reconnect/logout path and correctly reports disconnected state; server failures still propagate. Environment overrides direct the user to replace the override instead of implying a stored-login fix. | core auth service/tests, Settings presentation/tests | `dd7195b` |
| PCV-DEF-012: real-save acceptance | The user-created BOB save was backed up, copied to a second slot through Picori, restored through CLI and then GUI, and loaded in the real game in both directions. GUI restoration showed only BOB slot 1; restoring its automatic safety snapshot returned both BOB slots. Both loaded the blacksmith house with three hearts and 20 rupees. Final immutable verification passed after each GUI launch. Picori still aborts on quit; core collected the save and recorded failure honestly. | `docs/DEFERRED-CAMPAIGN.md`, `docs/DEFERRED.md`; private save identities and native observations | Evidence recorded in the completion documentation commit; fixes `24666f1`, `7a69d27`, `eb4b6f0` |

## Audit Phase 5

Phase 0–4 foundations were verified as landed after the independent audit baseline, including operation identity, recovery, migrations, artifact identity, supervised launch lifetime, and cross-process ownership. None of these capabilities introduces an adapter-owned domain authority.

| Capability | Final implementation status | Evidence, boundaries, files, commits |
|---|---|---|
| Library move/export/import + source relink | **Implemented** | Core plans bind reviewed inputs, capacity, metadata and file inventories; verified publication switches authority recoverably and retains originals. Import requires a separately trusted content bundle and empty destination. Source relink revalidates current profiles without modifying source bytes. Synthetic active/previous/staged round trips, interruption, stale plan, corruption, lock, and compiled CLI tests pass; real Paper Mario library move and launch recovery passed. `library_transfer/`, source service/CLI/Tauri adapters and transport contracts; `1268b6a`, `ce28ed6`, `1d030b5`, `47eeff6`. |
| Opt-in local source discovery | **Implemented** | Explicit roots/profiles, bounded traversal/hashing, symlink refusal, cheap candidate filtering, shared exact source validation, and explicit revalidated acceptance. Real selected-root scan found matching sources without registering them. No upload or background personal-folder scan. `source_discovery.rs`, CLI/Tauri/Settings adapters and tests; `ee73098`. |
| Phase-aware cancellation | **Implemented** | Durable activity UUID and cancellation outcome, cross-process ownership, bounded preparation checkpoints, and an atomic end-of-cancellation boundary before publication. Restore/migration/authoritative switches cannot be cancelled halfway. Fault, child-process, CLI and disappearing-control focus tests pass. Core cancellation/activity/service, CLI/Tauri and modal hooks; `462aeb5`, `519f660`. |
| Signed catalog updates with embedded fallback | **Implemented** | Explicit public-key trust, bounded delivery, Ed25519 verification, expiry/replay protection, state-bound review, rollback and offline embedded fallback. Executable/source/persistence contracts remain frozen. Offline publisher/compiled CLI interoperability used public test keys only. Core catalog trust/store, CLI/Tauri/Settings, publisher utility and tests; `aa440ce`. No production feed, key custody or relay was selected. |

## Remaining items

Every row below has completed actionable engineering or has an explicit prerequisite. Automation is not substituted for a physical observation or an unavailable source identity.

| ID | Engineering/evidence completed | Remaining condition and exact next step |
|---|---|---|
| PCV-DEF-002 | Local conditional release resolution and optional signed catalog delivery work independently. | **Infrastructure decision:** select hosting/public origin and signing ownership/rotation. Then define and review a separate advisory-event contract before deploying an optional relay. Catalog signing is not webhook/event delivery. |
| PCV-DEF-003 | 53 Windows automation badges, including NocturneRecomp and the eleven earlier ports completed during this campaign; existing seven manually qualified ports retain their evidence. Reports distinguish filesystem markers, process smoke, and real saves. | **Human gameplay qualification:** use an exact supported owned source and the catalog procedure; observe gameplay, audio, physical controls, and actual save/load in each pending port. A clean process or generated data cannot prove these. Final counts are recorded below. |
| PCV-DEF-004 | Live App authorization, secure credential persistence, GUI logout/login, links, rate display and restart were confirmed by the user. A later live 401 exposed a reconnect dead end. Core now preserves the rejected credential source while exposing sign-in recovery, and HTTP/component regressions pass (`dd7195b`); no credential is silently deleted or downgraded to anonymous use. | **Human edge-case observation:** cancel, deny, or allow one device code to expire; confirm readable recovery and then successful retry. Do not revoke a working credential merely to manufacture a failure. |
| PCV-DEF-005 | The reported controller bugs are fixed and physically improved; Move library containment and B/focus return passed. Async modal expansion/cancellation and keyboard containment have deterministic/browser evidence. | **Human Xbox judgment:** at minimum width, open a game's Advanced controls → Update policy; verify selection, one action per press, B closing only the choice, B returning to the card then sidebar, LB/RB sections, and focused-row visibility. |
| PCV-DEF-006 | Native WSL Linux tests exercise Unix source, symlink and process-lifetime paths independently of Windows. Platform badges are unchanged. | **Platform qualification:** select one declared Linux/macOS port and complete native bundle, graphics, audio, controller, lifecycle and shell checks on that OS. WSL core tests do not certify a Linux desktop; macOS hardware was unavailable. |
| PCV-DEF-007 | Every existing candidate plus the user's limited 2026-09-03 exception was reconsidered; 70 direct GitHub repositories plus applicable direct vendor/non-GitHub pages were rechecked. Existing generic multi-disc/file-set staging and new pinned runtime support were reused. Paper Mario, Mega Man X6, Severed Chains, Dinosaur Planet, Trouble Makers, Virtual Pro Wrestling 64, WWF WrestleMania 2000, Space Station Silicon Valley, all three OpenGOAL games and vanilla NocturneRecomp moved to manual qualification. Dino/SSSV completed visible launch and lifecycle checks with an explicit isolated Vulkan setting; their failed Direct3D runs remain recorded. | **Per-game source/upstream/platform prerequisites:** use the exact current blocker and resume condition in the two PCV-DEF-007 tables, supported by the direct-upstream inventory. No missing checksums were fabricated, sources weakened, global upstream installer registrations taken over, or failed runs promoted to badges. |
| PCV-DEF-009 | Real unsigned same-version predecessor/candidate replacement, hash identity, responsive clean launches, preserved library state and isolated uninstall passed. | **Signing and platform:** choose certificate/key custody and CI secret handling; sign a release candidate; qualify version-number upgrade and interactive SmartScreen/Gatekeeper/installer choices, shortcuts, icons and uninstall on target OSes. No credentials/custody were chosen on the user's behalf. |
| PCV-DEF-011 | Shared activity, persisted update badges, source gating, typed plans and volume data exist. Native production GUI checks and the final exact artifact are recorded in the evidence log. | **Human UI acceptance:** judge density/recovery copy and repeat the exercised workflows with Xbox at compact size. Tool-driven visible GUI evidence does not establish the user's controller feel. |
| PCV-DEF-013 | The Windows toolchain was revalidated with user participation. Optional semdup runs were expensive and did not justify blocking the campaign; other deep evidence remains available. The user-owned concurrent changes retire semdup from the project/host. | **Superseded by separate user-owned retirement:** those changes, including the PCV-DEF-013 queue deletion, were preserved and deliberately excluded from campaign commits. No successful semdup result is claimed. This is not an outstanding campaign implementation prerequisite. |
| PCV-DEF-014 | Fresh lockfile/dependency and direct advisory review confirms Tauri's Linux GTK3 path still selects GLib 0.18.5; the fixed line begins at 0.20. Portcove does not call the affected iterator directly. The visible informational advisory is not suppressed. | **Upstream:** a compatible fixed Tauri/GTK binding stack or narrowly reviewed independent backport is required. Upgrade and retest that path when available; Windows/macOS do not compile it. |

## Deliberate future work

Three reviewed product boundaries remain: a background queue requires persisted replayable intent, ordering, restart policy and reviewed authorization; mod recommendations and installation are explicitly postponed to post-V1 by the user (2026-09-03) and require provenance, ownership/conflicts and executable/update/rollback policy; desktop self-update requires signing custody and qualified staged replacement/rollback. Existing activity history, mutable mod folders, and signed catalog metadata do not supply those contracts. See the concrete prerequisite table in [the archived deferred ledger](2026-09-03-deferred-ledger.md).

## Human steps, in dependency order

1. Supply only the legally owned exact revisions/profile sets still listed under PCV-DEF-007; do not modify or upload originals. For source-contract gaps, obtain a direct maintainer-verifiable identity first.
2. Complete the remaining GitHub error/retry and minimum-width Xbox nested-navigation checks in the current qualified desktop.
3. Perform gameplay/audio/controls and each port's own save/load test for the ports being certified; the cross-frontend backup/restore acceptance is complete. Record the tested artifact/platform; change manual badges only after observation.
4. Provide a supported native Linux/macOS test environment for the platform pairs being certified.
5. Choose signing identity/custody and target release platforms; after signed candidates exist, observe interactive installer/security/shell behavior and a real version-number upgrade.
6. If an update relay is desired, choose hosting/origin and signing custody. Otherwise leave the optional relay deferred; local resolution remains usable.

## Final verification and remaining counts

Code/artifact checkpoint: this completion commit. The full Windows `just check-rust`, `just check-ui`, `just audit`, and `just deep` passed after the Gen2Recomped and NocturneRecomp catalog/source-contract additions. `just audit` and `just deep` repeat the combined Rust/UI gate; those gates were not treated as skipped just because the broader entry points were used.

| Verification | Exact result |
|---|---|
| Windows Rust | 243 core, 19 CLI unit, 14 compiled CLI contract, 8 Tauri adapter tests passed; fmt, workspace check, Clippy, unused dependencies, architecture, child-process policy and transport checks passed. |
| UI | 88 tests across 18 files passed; TypeScript/Vite production build and theme checks passed. Fallow maintainability 86.8; duplication 0.504626%; gate passed. |
| Native Linux, Rust 1.88 / WSL Ubuntu | 241 core, 19 CLI unit and 14 compiled CLI contract tests passed in a fresh source snapshot. The first parallel attempt collided with rustup component installation inside process-helper tests; an isolated minimal toolchain and serial full rerun passed. No native Linux desktop or game qualification is implied. |
| Audit/deep | Dependency policies passed with visible informational/unmaintained advisories. The recorded `PortcoveError`/inherent-constructor cycle remains advisory. rscheck: 15 advisory findings, zero blocking. Hawk: explicitly skipped because pinned cargo-hawk 0.1.13 does not support Windows. Semdup retirement belongs to separate user-owned changes; no successful run is claimed. |
| Production desktop | A prior clean campaign snapshot produced the Tauri custom-protocol release build and NSIS package. The raw desktop SHA-256 was `71ac9b4cf0fa2e43368bb522e1767d4c32ec039405afbab372da68642bcb1929`; its expected NSIS-patched identity was `dbd3cd8060945195e543de9c4085e2ab24566c3d92e532f24becdb57c2ffabfb`. Earlier native GUI real-save restore and automatic safety recovery remained accepted by Picori. The Gen2/Nocturne completion commit passed the full source-build and deep-quality gates recorded above; it was not repackaged. |
| Installer | The prior unsigned installer installed into a new isolated directory, published the exact expected bundled executable, opened a responsive `Portcove` window, exited 0, preserved the test marker, uninstalled with exit 0, and removed its application files and registration. Earlier same-version predecessor-to-candidate replacement also passed. |
| Release CLI | The prior clean-snapshot optimized build passed and `about` emitted a valid schema-9 envelope. The current API contract is schema 10 and the embedded catalog contains 65 ports, including the three source-pending Gen2Recomped entries and the automated Windows NocturneRecomp entry. |

Private logs: `final-source-ownership-check.log`, `final-source-ownership-linux.log`, `final-deep.log`, `desktop-source-ownership-build.log`, `final-cli-release-build.log`, and `final-installer-source-ownership-retry.log` under `work/deferred-campaign/`. Earlier totals in the chronological log are historical.

| Artifact | SHA-256 |
|---|---|
| Windows release CLI | `59f56ee959430ef2c2c91da874ca40ee98f5e3a7f44dd41766ade932cd61d684` |
| Desktop executable before NSIS bundle-type patch | `71ac9b4cf0fa2e43368bb522e1767d4c32ec039405afbab372da68642bcb1929` |
| NSIS-bundled executable | `dbd3cd8060945195e543de9c4085e2ab24566c3d92e532f24becdb57c2ffabfb` |
| Unsigned Windows installer | `0ee352209e1c18df0da5b23161d34dc1ebb483ed7ba627c6998c89ebdd85dcd3` |

Local package hashes belong to `outputs/SHA256SUMS.txt`. The source archive is a Git commit snapshot; concurrent user-owned quality-tool and artwork work is excluded. Old local packages are preserved privately before replacement. No artifact is uploaded or published.

Counts use one primary blocker per unresolved queue item: each PCV-DEF ID except 007 counts once, each of 007's 23 game rows counts once, and each explicit future product decision counts once. A row with multiple prerequisites is assigned to the first practical blocker, while its full conditions remain in DEFERRED.md. PCV-DEF-003 counts as one manual program containing 45 ports; its ports are not counted again as 45 queue items.

| Primary blocker | Count | Membership |
|---|---:|---|
| Manual | 4 | PCV-DEF-003, 004, 005, 011 |
| Source-dependent | 11 | VPW2; FFVII; G-Diffuser; OpenPete; Gen2 Gold, Silver, and Crystal; Nocturne TU; Kameo; reDAHM; SVR07. Exact identity and deterministic setup prerequisites still apply where listed. |
| Upstream-dependent | 17 | PCV-DEF-014 plus Ghostship, Starship, Dr. Mario 64, re:Blue, WipeOut, Cannonball, both ProjectR games, both R.E.L.I.V.E. games, Conker64, Star Fox Enhanced, Chameleon Twist, Sonic Unleashed, Zelda3, and Zelda: Link's Awakening DX HD. |
| Credentials/signing/infrastructure | 2 | PCV-DEF-002 and 009 |
| Platform qualification | 2 | PCV-DEF-006 and DKR-R |
| Intentional future work | 3 | Background queue, mod packages/recommendations/browser, desktop self-update |
| Optional tooling | 0 | PCV-DEF-013 is superseded by separate user-owned retirement. |
| **Total** | **39** | PCV-DEF-012, the three OpenGOAL source blockers, and vanilla NocturneRecomp are complete and removed from the remaining queue; the Nocturne title-update variant remains explicit. |

The embedded catalog contains 65 ports: 53 have automated Windows evidence, seven have manual Windows evidence, and 46 automated ports still need their own hands-on gameplay/audio/controller/save qualification. The three Gen2Recomped entries have no automation badge until matching ROMs can exercise the real importer. No manual badge was added by this campaign.
