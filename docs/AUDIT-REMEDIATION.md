# Independent audit remediation ledger

This ledger tracks the remediation of `PCV-AUD-001` through `PCV-AUD-039` from the corrected independent audit pinned to `02654c757c24a8eabc0c518a1aea305264944070`. The remediation branch began at that exact clean commit. A row marked **In progress** is not a final disposition; it records the next dependency-safe phase while implementation continues. Final dispositions are limited to Resolved, Already resolved, Superseded, Deferred with an external blocker, or Rejected with evidence.

## Baseline

- Branch: `codex/portcove-audit-remediation`
- Audited/current starting commit: `02654c757c24a8eabc0c518a1aea305264944070`
- `just check`: passed before edits (137 Rust tests, 64 frontend tests, architecture, Fallow, theme, release tooling).
- `just audit`: passed before edits with zero blocking findings. The documented cargo-modules inherent-item cycle, GTK3/`proc-macro-error`/`unic-*` advisories, and rscheck shape findings remained advisory.
- Optional baseline: `just mutants` not run before implementation; `just deep` was not repeated because the audit and `docs/QUALITY.md` already record the exact hosted baseline and the current Windows semdup linker blocker.

## Findings

| ID | Current disposition | Verified evidence / implementation | Files or modules | Proof | Dependencies / external follow-up | Commit |
|---|---|---|---|---|---|---|
| PCV-AUD-001 | Resolved | Direct `Command::new` callers inherited the parent environment. Added one core-owned typed policy with a reviewed session allowlist and centralized removal of GitHub and credential-shaped variables; all production child sites now route through it. | `process.rs`, CLI, Tauri, `adapter.rs`, `psx.rs`, architecture/security/CLI docs | Helper-process environment test, five-class credential-boundary test, policy bypass gate, `just check-rust`, `just check`, and `just audit` pass. | Foundational boundary is complete; 003 and 019 build on it. | `65e14fe` |
| PCV-AUD-002 | Resolved | Lifecycle intent now persists in SQLite outside staging. Install/adopt publish only verified private payloads; removal quarantines every managed version before metadata deletion; restore and staged activation use the same explicit phase model. Startup resumes unambiguous states and retains ambiguous failures for the read-only repair plan. | `operation.rs`, `recovery.rs`, `install.rs`, `service.rs`, `library.rs`, migration 7, architecture/security docs | Restart tests cover every injected install/adopt/removal boundary, restore before/after publication, all four unambiguous half-published restore states, preparing removal, and both activation identity branches. Existing retained/staged reuse tests remain green; repair-plan tests prove no deletion. `just check`, `just audit`, final `just check-rust`, and targeted mutation testing pass. | Uses completed 007/019 foundation. Deterministic fault injection covers retryable cleanup; CI cannot portably force antivirus locks or full disks, so those host-specific cases remain platform coverage limitations rather than untested state transitions. | `9c9a923` |
| PCV-AUD-003 | Resolved | Core now owns spawn, durable session/PID/install identity, process-group policy, wait, launch outcome, exact-install collection, and session cleanup under one port lock. Desktop requests a detached native supervisor and merely observes the row; startup recovers stale sessions. CLI forwards interrupts while retaining supervision. | `launch.rs`, `service.rs`, `library.rs`, migration 9, CLI, Tauri, architecture/security/CLI docs | Helper-process tests cover success, nonzero exit, failed spawn, active lock contention, forwarded interruption, stale pre-spawn recovery, and stale live-child recovery against an inactive exact version. `just check` passes. | Uses completed 001/002/006/019. PID reuse is handled conservatively by leaving the port blocked rather than risking concurrent mutation. | `8546c35` |
| PCV-AUD-004 | Resolved | Reconcile resolves optimistically outside the lock, then rereads channel, policy, active, previous, and staged state after acquiring the port lock. One channel change triggers one bounded re-resolution; a second change fails with a typed conflict. No pre-lock policy or staged decision is applied. | `service.rs` | Blocking-provider race tests prove post-lock automatic staged activation and stable-to-beta retry/order. The service suite and `just check` pass. | Establishes the concurrent-mutation snapshot boundary used by later bulk work. | `8546c35` |
| PCV-AUD-005 | Resolved | One core archive engine now preflights ordinary release and private-toolchain ZIP/TAR payloads before writing. It enforces compressed/expanded/per-entry/count/ratio/path/depth/free-space bounds, regular file/directory types, portable ASCII paths, duplicate/case-fold/file-directory collision keys, Windows device/ADS/trailing aliases, and exact streamed sizes. | `archive.rs`, `install.rs`, `psx.rs`, architecture/security docs | Malicious ZIP/TAR fixtures cover traversal, symlink/hardlink/FIFO/device types, duplicates/case aliases, `CON`/`NUL`, ADS, trailing aliases, deep/long paths, high ratios, declared/stream size failure, and no-write structural rejection. Core and UI suites pass. | Uses 002 private staging/retry boundary. Host free-space behavior is checked through the real containing-volume API; portable tests do not consume a volume to force ENOSPC. | `ffbcbbb` |
| PCV-AUD-006 | Resolved | Schema-2 install manifests bind durable install/artifact/executable identity and immutable file bytes. Qualification persists the catalog-selected executable without fallback. Launch, active/staged/retained reuse, activation, rollback, and recovery validate manifest identity and rehash the selected executable plus same-directory libraries/bootstrap configuration before side effects; mutable catalog paths are excluded. PS1 toolchain markers bind Python/CMake/Ninja/metadata bytes. | `install.rs`, `service.rs`, `adapter.rs`, `psx.rs`, `types.rs`, migration 8 | Active executable/library/manifest, database identity, staged, retained, rollback, missing-selected/fallback, and toolchain tamper tests fail before launch markers or pointer changes; mutable configuration still verifies and launches. | Uses completed 002/007/011. Full verification remains explicit so source/runtime bulk data is not rehashed on every launch. | `ffbcbbb` |
| PCV-AUD-007 | Resolved | A library-scoped OS file lock serializes migration across processes. Eight explicit version-gated steps run in immediate SQLite transactions, verify structural postconditions before recording, reject gaps/future schemas, and preserve WAL/busy timeout as separate connection behavior. | `database.rs`, `library.rs`, `operation.rs`, `docs/ARCHITECTURE.md` | Fresh and every historical schema, four simultaneous opens, future version, recorded partial table/column/index, and idempotent unrecorded partial-step tests pass. Migration postcondition mutants are caught. | Foundation consumed by 002/006/011; diagnostics feed 017. | `9c9a923` + Phase 0C migration |
| PCV-AUD-008 | Resolved | Status reads optional settings and supplies catalog defaults in memory; status and doctor never insert per-port rows. | `library.rs`, `service.rs`, CLI/architecture docs | Fresh-library status/doctor test proves the settings table remains empty while beta-only and rolling-only defaults are returned. | Query-count optimization remains separately tracked by 022. | Phase 1 commit pending |
| PCV-AUD-009 | Resolved | Policy initialization now receives the core catalog default instead of hard-coding stable. Stable is preferred when offered; otherwise the first declared channel remains authoritative. | `library.rs`, `service.rs` | A policy-only write for beta-only DKR preserves beta in SQLite and status. Rolling is covered by the read-only default test. | Uses 008's non-mutating read model. | Phase 1 commit pending |
| PCV-AUD-010 | Resolved | `AdapterKind::ALL` is the single capability inventory, includes staged-source portable, and is checked against every adapter represented by the embedded catalog. | `types.rs`, `service.rs`, CLI transport contract | Focused catalog/capability set-equality test and schema-to-TypeScript enum check pass. | Consumed by 012. | Phase 1 commit pending |
| PCV-AUD-011 | Resolved | `ArtifactIdentity` persists asset name, verified SHA-256, and byte size for every channel. Display tags remain human-readable and separate; status/update machine output exposes installed and resolved identities. Update checks, plans, reuse, activation, and rollback compare artifact identity rather than version text. Migration 8 leaves legacy provenance empty instead of fabricating it. | `types.rs`, `release.rs`, `library.rs`, `service.rs`, migration 8, CLI/architecture docs | A same-tag/different-digest regression proves the republish is an update, both artifacts coexist, staged activation and rollback preserve the same display version while switching digests, and plan/machine results expose the distinction. | Foundation for 006/032. Legacy installs require replacement or re-adoption before trusted reuse. | `ffbcbbb` |
| PCV-AUD-012 | Resolved | Library-free `schema export` is the transport authority. A deterministic check compares top-level Rust DTO fields, adapter and shared enum values, and operation-event variants with desktop TypeScript declarations as part of `just check-rust`. | CLI schema, `types.ts`, `check-transport-contract.mjs`, Justfile | The gate exposed and corrected missing launch/source-removal operations, source timestamps, catalog release/executable fields, and the retired `direct-archive` fixture; frontend compile/tests pass. | Future additive DTO or enum drift now fails the standard gate. | Phase 1 commit pending |
| PCV-AUD-013 | Resolved | Initial source verification is followed by a full profile identity check after adapter preparation and immediately before spawn. Managed PS1 preparation rechecks source storage after disc materialization and BIOS storage after generator use. | `service.rs`, `adapter.rs`, `psx.rs` | Deterministic fault injection replaces a registered source during adapter preparation and proves a typed source failure before marker/spawn. Existing changed-source tests remain green. | Uses 002 private preparation and 003 core spawn boundary. | Phase 1 commit pending |
| PCV-AUD-014 | Resolved | V1 now explicitly rejects non-Unicode paths at library, durable SQLite/JSON, source/member, backup, lifecycle, launch argument/environment, and managed build boundaries rather than storing lossy aliases. | `path.rs`, `library.rs`, `operation.rs`, `adapter.rs`, `psx.rs`, `service.rs`, docs | Unix-only helper and library API tests construct invalid UTF-8 paths and prove `unsupported` with no library side effects; Windows workspace checks pass. | Native non-Unicode path support is intentionally outside V1. | Phase 1 commit pending |
| PCV-AUD-015 | In progress — Phase 2 queued | Adoption preview/copy plan and skipped-entry reporting require two-step workflow. | core service/types, Tauri, React | Pending plan-token and component flow tests. | After 002 and 016. | pending |
| PCV-AUD-016 | In progress — Phase 2 queued | Destructive commands still rely on renderer confirmation at the starting commit. | Tauri authorization, React confirmation | Pending expiry/replay/state-change tests. | Shared with 015/028. | pending |
| PCV-AUD-017 | Resolved | Desktop initialization stores either ready services or a typed failure; all domain commands fail closed through that state while React displays error details, recovery guidance, and retry without exposing library actions. Recoverable startup paths no longer panic. | Tauri bootstrap, `App.tsx`, styles/types/API | A file-blocked library fixture proves bytes remain untouched and commands stay disabled; server-rendered UI test proves the recovery surface. | App-runtime creation failures still report to stderr because no webview exists to present them. | Phase 1 commit pending |
| PCV-AUD-018 | In progress — Phase 2 queued | Refresh generations and per-operation state require operation identity first. | React hooks/types | Pending reverse-completion/overlap tests. | After 019. | pending |
| PCV-AUD-019 | Resolved | The core-owned versioned event envelope carries the durable activity UUID, monotonic per-operation sequence, timestamp, typed target, optional parent ID, and terminal success/failure. Nested setup gets a child coordinator; CLI and Tauri translate the same envelope. React stores operations independently and rejects stale sequences instead of treating one global event as authority. | `operation.rs`, `types.rs`, `service.rs`, `psx.rs`, CLI, Tauri, `operation-state.ts`, desktop types/docs | Core overlap/nesting and durable-ID tests, failed-operation terminal-event test, frontend reordering/nested-correlation tests, `just check`, `just audit`, and final `just check-rust` pass. | Enables 003/018/039; Phase 2 still owns refresh generations and busy/error presentation state. | `9c9a923` |
| PCV-AUD-020 | In progress — Phase 2 queued | Modal gamepad scope and one Back stack require interaction revalidation. | gamepad/dialog/overlay hooks | Pending modal focus/back tests. | Coordinate with 018. | pending |
| PCV-AUD-021 | In progress — Phase 2 queued | No top-level render boundary at the starting commit. | React entry/shell | Pending injected-render-failure tests. | Integrate with diagnostics. | pending |
| PCV-AUD-022 | In progress — Phase 3 queued | Status/bootstrap query counts require bulk read-model instrumentation. | `library.rs`, `service.rs`, Tauri | Pending 250/500/1,000-record measurements. | With 008/012. | pending |
| PCV-AUD-023 | In progress — Phase 3 queued | CLI/Tauri bulk checks require bounded provider concurrency. | core/CLI/Tauri check paths | Pending bound/order/rate-limit tests. | After blocking/operation boundaries. | pending |
| PCV-AUD-024 | In progress — Phase 3 queued | Blocking filesystem/hash/process phases require explicit worker boundaries. | core service/install, Tauri | Pending responsiveness/cancellation-boundary tests. | After 002/019. | pending |
| PCV-AUD-025 | In progress — Phase 4 queued | Host audit found unprotected `main`; repository artifact and admin command required. | ruleset JSON/script/docs | Pending exact settings artifact/API validation. | Admin application may be external. | pending |
| PCV-AUD-026 | In progress — Phase 4 queued | Matrix jobs currently mutate one draft without aggregate reconciliation. | release workflow/scripts/tests | Pending missing/duplicate/hash/rerun tests. | With 025/038. | pending |
| PCV-AUD-027 | In progress — Phase 4 queued | Normal Rust toolchain floats while MSRV is 1.88. | toolchain, CI, metadata docs/tests | Pending pinned and MSRV checks. | Dependency MSRV review. | pending |
| PCV-AUD-028 | Resolved | Source removal is a two-step core workflow. The preview reports exact source identity, every catalog dependent, installed dependents, and a token binding all three; removal locks dependents and rejects source or impact changes before deleting only the reference. CLI requires confirmation/`--yes`. | core service/library/types, CLI/schema/docs | Shared Banjo source test covers two dependents, installed-impact change, source replacement, stale-token rejection, and exact successful deletion. | Tauri has no source-delete caller; future UI exposure must call the same preview/token API. Shared destructive authorization remains tracked by 016. | Phase 1 commit pending |
| PCV-AUD-029 | Resolved | One generic selector now defines GitHub and GitLab semantics: stable excludes prereleases, beta prefers an eligible prerelease then falls back only to eligible stable, and rolling requires its exact configured tag. | `release.rs`, `gitlab.rs`, CLI docs | Shared selector test covers beta, stable fallback, unavailable/draft exclusion, and exact rolling selection; both providers call only that selector. | Uses immutable artifact comparison from 011 after selection. | Phase 1 commit pending |
| PCV-AUD-030 | In progress — final sweep queued | Human output is transport JSON; safe read-command renderers will be assessed after contract changes. | CLI rendering/docs | Pending snapshots; machine contract must remain stable. | Optional polish after higher-priority contracts. | pending |
| PCV-AUD-031 | Resolved | CLI device login uses Tokio timer/signal selection instead of blocking the runtime, returns a typed cancellation, and retains provider-enforced next-poll/slowdown/expiry/denial behavior. | CLI auth loop, GitHub provider, docs | Async wait test proves immediate cancellation and timer completion; local HTTP tests cover authorization pending, slowdown backoff, remote/local expiry, and access denial. | No remaining blocking sleep in the CLI auth loop. | Phase 1 commit pending |
| PCV-AUD-032 | Resolved | Version directories use the complete lowercase verified artifact SHA-256. Display versions, including long, Unicode, or path-like upstream tags, never participate in directory construction. | `install.rs`, `service.rs`, `library.rs`, architecture docs | Canonical-key tests reject non-digests, same-tag republish tests create distinct paths, and lifecycle recovery tests use the content-addressed layout. | Resolved through 011; no lossy display-tag sanitization remains. | `ffbcbbb` |
| PCV-AUD-033 | In progress — final sweep queued | Backup publication wording must distinguish process atomicity from power-loss durability. | architecture/CLI/service | Pending documentation reconciliation and supported directory-sync proof. | No blocker. | pending |
| PCV-AUD-034 | In progress — Phase 2 queued | Command-palette accessible name requires exact DOM test. | `CommandPalette.tsx` | Pending role/name test. | With 020/021 accessibility pass. | pending |
| PCV-AUD-035 | In progress — Phase 4 queued | Security policy lacks a concrete private GitHub reporting route. | `SECURITY.md`, repository setting docs | Pending checked-in guidance; enabling setting may need admin. | External host-setting validation possible. | pending |
| PCV-AUD-036 | In progress — Phase 4 queued | Quality pins are duplicated across scripts/workflows/prose. | machine pin manifest/bootstrap/workflows/docs | Pending stale-consumer tests. | Tooling pass after correctness work. | pending |
| PCV-AUD-037 | Resolved | `.bat`/`.cmd` is now an explicit launch kind. Caller arguments are rejected; fixed catalog arguments reject shell metacharacters; non-game process classes require native executables. | `process.rs`, adapter/CLI/Tauri, CLI/security/architecture docs | Hostile argument matrix and real Windows batch execution tests pass; policy bypass gate, `just check-rust`, `just check`, and `just audit` pass. | Shares the completed 001 policy. | `65e14fe` |
| PCV-AUD-038 | In progress — Phase 4 queued | Release workflow write permission must move to one final publisher. | release workflow/tests | Pending effective-permission/rehearsal tests. | With 026. | pending |
| PCV-AUD-039 | In progress — Phase 2 queued | Desktop lacks durable redacted tracing at the starting commit. | Tauri logging, support bundle | Pending rotation/redaction/restart tests. | With 019 IDs. | pending |

## Phase evidence log

### Phase 0A — child-process and credential boundary

- Baseline search found direct production process creation in CLI game launch, Tauri game launch and folder integration, upstream setup, managed PS1 building, `chdman`, and DolphinTool.
- `ChildProcessPolicy` now owns environment construction for all of them. `LaunchKind` makes native and Windows batch launches explicit.
- `scripts/check-child-process-policy.mjs` is part of `just check-rust` and rejects future direct production `Command::new` bypasses.
- Proof: `cargo test -p portcove-core process::tests -- --nocapture`, `cargo check --workspace --all-targets`, `just check-rust`, `just check`, and `just audit` passed. The final audit ran 143 Rust tests and 64 frontend tests, reported zero blocking cargo-deny or rscheck findings, and retained only the documented cargo-modules advisory cycle and dependency/shape advisories.

### Phase 0B — durable operations and database foundation

- `database.rs` is the single migration authority. `locks/migration.lock` supplies cross-process serialization; each of seven ordered steps is transactional and records its version only after structural verification.
- `OperationCoordinator` reuses durable activity IDs and emits a versioned, sequence-aware envelope. Nested PSX setup carries its parent ID. The desktop reducer retains independent operation state and ignores stale events.
- Durable lifecycle rows record install, adoption, removal, restore, and staged-activation intent and phase. `recovery.rs` resumes only path/identity states whose outcome is unambiguous; errors remain journaled and become repair-plan items.
- Installation and adoption validate a private same-volume tree before rename. Removal renames registered managed versions into `recovery/` before deleting metadata. Restore preserves the prior live tree until the replacement is published. Startup cleanup is retryable and never treats an untracked final directory as disposable.
- Focused database and service suites pass, including fresh and historical migration, four simultaneous migrators, future/partial schemas, every injected publication boundary, half-published path matrices, retained/staged reuse, and read-only repair reporting.
- `just check` passed with 157 Rust and 66 frontend tests before the final mutation additions; `just audit` passed with zero blocking cargo-deny or rscheck findings and only the recorded dependency/shape/cargo-modules advisories. Final `just check-rust` passed with 162 Rust tests.
- Targeted cargo-mutants coverage selected 41 state-transition mutations. The first run caught 28, exposed 12 meaningful coverage gaps, and produced one compiler-rejected mutation. After adding exact corruption and recovery-matrix tests, all 13 prior survivors/rejected cases were caught. Windows symlink privilege prevented isolated-copy mode, so the runs used cargo-mutants' transactional in-place mode followed by `git diff --check`; its generated reports were removed after evidence was recorded.
- Host-specific antivirus/open-handle and read-only/full-disk failures are represented by deterministic boundary injection and retryable persisted states. Portable CI cannot force those OS conditions reliably; no destructive repair behavior is enabled on that basis.

### Phase 0C — immutable artifact and archive trust

- Schema 8 adds an asset name, verified artifact SHA-256 and size, manifest SHA-256, and catalog-selected executable to every new install. Existing rows receive no invented identity and therefore fail current-integrity gates until replaced or re-adopted.
- Full artifact digests own version-directory keys. Display tags remain unchanged across stable, beta, rolling, and direct-manifest releases; update checks, install plans, active/staged matching, and retained reuse compare artifact identity.
- Schema-2 manifests bind port/install/display/artifact/executable identity and every immutable file. The hot critical set contains the selected executable and same-directory libraries, launchers, and bootstrap configuration; declared saves and mutable configuration remain outside it.
- Active/staged/retained reuse, activation, rollback, lifecycle recovery, and launch revalidate the manifest and current critical bytes before pointer, persistence, marker, or process side effects. The PS1 toolchain cache applies the same principle to Python, CMake, Ninja, and toolchain metadata.
- One shared archive engine preflights release and toolchain ZIP/TAR packages with compressed, expanded, per-entry, count, ratio, path length/depth, entry-type, portable collision, and free-space policy before extraction. Downloads enforce declared and global compressed sizes while streaming.
- Focused proof covers same-tag republishing, active executable/library/manifest and database-identity tamper, staged/retained/rollback tamper, no executable fallback, mutable-file exclusion, toolchain tamper, and hostile ZIP/TAR cases. Final `just check` and `just audit` passed with 153 core tests, 22 CLI tests, 66 frontend tests, architecture/process/release gates, zero blocking cargo-deny findings, and zero blocking rscheck findings. The recorded cargo-modules inherent-item cycle, dependency advisories, and shape findings remain advisory.

### Phase 0D — launch supervision and reconcile snapshots

- Schema 9 adds one durable launch session per port, bound to the launch activity, supervisor PID, child PID, exact registered install identity/root, and explicit preparing/running/collecting/recovering phase. A durable session continues to reject every normal port lock even after an operating-system lock is released by a crashed process.
- `PortcoveService::supervise_launch` is the single owner of preparation, child construction, PID persistence, post-spawn marker creation, waiting, successful-exit history, exact-install save collection, and cleanup under the port lock. A failed spawn creates no marker or unfinished session; a nonzero/signal exit still collects saves but never increments successful history.
- The desktop writes an operation-private request and launches a detached copy of its native executable in hidden supervisor mode. Tauri receives the durable session/child identity and may observe completion, but does not own the child or save lifecycle. Startup starts equally detached recovery helpers for stale rows; recovery waits a still-live recorded child, validates the registered exact install, collects it, and records the interrupted activity as failed.
- CLI `exec` runs the same core supervisor on a blocking worker, puts native games in their own process group, forwards Ctrl-C and Unix termination, and remains alive for collection. The child-process gate still finds no construction bypass.
- Reconcile now performs network release lookup outside the port lock, acquires the lock, and rereads channel, policy, active/previous, and staged state. It retries one changed channel exactly once, then conflicts instead of applying an unbounded or stale decision.
- Focused proof covers success, nonzero exit, failed spawn, active lock contention, forwarded interruption, stale pre-spawn recovery, stale live-child recovery from the recorded inactive version, post-lock policy/staged changes, and stable-to-beta channel retry. `just check` passed with 161 core tests, 22 CLI tests, 66 frontend tests, and all architecture, process-policy, Fallow, theme, and release gates.

### Phase 1 — read models, transport, source, path, bootstrap, release, and authentication contracts

- Status and doctor read optional settings without inserting defaults. Catalog-only beta and rolling channels remain visible, and a later policy-only mutation persists the same catalog default.
- `AdapterKind::ALL` owns the advertised adapter inventory and is set-equal with catalog use. Library-free schema export now feeds a deterministic Rust-to-TypeScript contract gate for DTO fields, shared enums, adapters, and operation-event variants.
- Registered and explicit launch sources receive a final catalog-profile identity check after adapter preparation and before spawn. Managed PS1 generation rechecks source storage after disc materialization and BIOS storage after generator use.
- V1's cross-platform path contract is explicit: paths crossing durable serialization or process-string boundaries must be Unicode. Unix-only invalid-byte tests prove unsupported rejection before library creation.
- Desktop initialization retains a typed failed state instead of panicking on library, recovery, or provider errors. Tauri commands fail closed and React renders a recovery-only retry surface.
- Source removal previews every catalog and installed dependent and requires a token bound to source and impact state. Shared-source installation or source-identity changes invalidate consent; successful removal deletes only the reference.
- GitHub and GitLab share exact stable, beta-with-stable-fallback, and rolling-tag selection semantics. CLI device polling uses an async timer/cancellation boundary and provider tests cover pending, slowdown, expiry, and denial.
- Final `just check` and `just audit` passed with 168 core tests, 17 CLI unit tests, 6 CLI machine-contract tests, one desktop bootstrap fixture, 67 frontend tests, and all architecture, child-process, transport, Fallow, theme, release, cargo-deny, and rscheck gates. Only the already recorded cargo-modules inherent-item cycle, dependency advisories, and shape advisories remain non-blocking.
